
create

FOR EACH of sRNAs, deg reads:
    remove t/rRNA




map exact



make overall hgram and stats
Normalise each sample
pick peaks

select distinct category < 2 peaks
select disticnt sirnas, filter

map to siRNA

loop thru data, keep only replicated peaks


